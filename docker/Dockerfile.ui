# docker/Dockerfile.ui

FROM python:3.11-slim

WORKDIR /app

# Only UI dependencies
RUN pip install --no-cache-dir streamlit==1.35.0 requests==2.32.3

# Copy UI code
COPY ui/ ./ui/

# Create Streamlit config
RUN mkdir -p /root/.streamlit
RUN echo '\
[server]\n\
port = 8501\n\
headless = true\n\
enableCORS = false\n\
[theme]\n\
base = "dark"\n\
primaryColor = "#7c8cf8"\n\
backgroundColor = "#0f1117"\n\
secondaryBackgroundColor = "#1e2130"\n\
textColor = "#ffffff"\n\
' > /root/.streamlit/config.toml

EXPOSE 8501

HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
    CMD python -c "import requests; requests.get('http://localhost:8501/_stcore/health')" \
    || exit 1

CMD ["streamlit", "run", "ui/main.py", "--server.address", "0.0.0.0"]